import { formValidationStyles } from '@styles/formStyles'
import { theme } from '@styles/theme'
import styled from 'styled-components'

export const FormValid = styled.span`
  ${formValidationStyles}
  color: ${theme.Green};
`
